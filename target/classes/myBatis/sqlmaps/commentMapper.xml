<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Comment">
    <select id="commentList"
            parameterType="Integer"
            resultType="com.green.vo.CommentVo">
        SELECT
            _id,
            username,
            time,
            content
        FROM
            comment_tb
        WHERE
            content_id = #{content_id}

    </select>

    <insert id="commentWrite">
        INSERT INTO  comment_tb(content, username, content_id, time)
        VALUES (#{content}, #{username}, #{content_id}, #{time})
    </insert>

    <insert id="commentUpdate">
        UPDATE comment_tb
        SET content = #{content}
        where     _id      = #{_id}
        and      username  = #{username}
    </insert>


    <delete id="commentDelete">
        DELETE FROM comment_tb
        WHERE _id  = #{_id}
        AND   username= #{username}

    </delete>

</mapper>
